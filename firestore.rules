rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only the backend Admin SDK can bypass these rules.
    // All client-side direct access is denied.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
